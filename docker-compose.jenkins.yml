

services:

  backend:
    build:
      context: ./backend

    ports:
      - "5002:5000"


  frontend:
    build:
      context: ./frontend

    ports:
      - "3002:3000"

    depends_on:
      - backend